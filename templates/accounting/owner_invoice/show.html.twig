{% extends 'acct-base.html.twig' %}

{% block title %}Owner Invoice{% endblock %}

{% block content %}
    <h1>Owner Invoice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ owner_invoice.id }}</td>
            </tr>
            <tr>
                <th>DueDate</th>
                <td>{{ owner_invoice.dueDate ? owner_invoice.dueDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PaidDate</th>
                <td>{{ owner_invoice.paidDate ? owner_invoice.paidDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PostDate</th>
                <td>{{ owner_invoice.postDate ? owner_invoice.postDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accounting_owner_invoice_index') }}">back to list</a>

    <a href="{{ path('app_accounting_owner_invoice_edit', {'id': owner_invoice.id}) }}">edit</a>

    {{ include('accounting/owner_invoice/_delete_form.html.twig') }}
{% endblock %}
