<form method="post" action="{{ path('accounting_ledger_type_delete', {'id': ledger_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ledger_type.id) }}">
    <button class="btn">Delete</button>
</form>
