{% extends 'acct-base.html.twig' %}

{% block title %}Edit LedgerAccount{% endblock %}

{% block content %}
    {{  parent() }}
    <h1>Edit Ledger Account</h1>
    {{ form_start(form) }}    
    {{ include('accounting/ledger_account/_form.html.twig') }}
    <div class="row">
        <div class="col-sm-3">
            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
            <li class="btn btn-info"><a href="{{ path('accounting_ledger_account_index') }}">back to list</a></li>
            {{ include('accounting/ledger_account/_delete_form.html.twig') }}
        </div>
    </div>
    {{ form_end(form) }}          

{% endblock %}
