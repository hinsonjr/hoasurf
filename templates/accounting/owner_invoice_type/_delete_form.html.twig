<form method="post" action="{{ path('app_accounting_owner_invoice_type_delete', {'id': owner_invoice_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ owner_invoice_type.id) }}">
    <button class="btn">Delete</button>
</form>
