{% extends 'acct-base.html.twig' %}

{% block title %}Owner Invoice Type{% endblock %}

{% block content %}
    <h1>Owner Invoice Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ owner_invoice_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ owner_invoice_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accounting_owner_invoice_type_index') }}">back to list</a>

    <a href="{{ path('app_accounting_owner_invoice_type_edit', {'id': owner_invoice_type.id}) }}">edit</a>

    {{ include('accounting/owner_invoice_type/_delete_form.html.twig') }}
{% endblock %}
