{% extends 'acct-base.html.twig' %}

{% block title %}HoaReportCategory{% endblock %}

{% block content %}
    <h1>HoaReportCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hoa_report_category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ hoa_report_category.category }}</td>
            </tr>
            <tr>
                <th>Ordering</th>
                <td>{{ hoa_report_category.ordering }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hoa_report_category_index') }}">back to list</a>

    <a href="{{ path('app_hoa_report_category_edit', {'id': hoa_report_category.id}) }}">edit</a>

    {{ include('accounting/hoa_report_category/_delete_form.html.twig') }}
{% endblock %}
