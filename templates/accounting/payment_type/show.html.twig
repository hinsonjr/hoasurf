{% extends 'acct-base.html.twig' %}

{% block title %}Payment Type{% endblock %}

{% block content %}
    {{  parent() }}
    <h1>Payment Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ payment_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accounting_payment_type_index') }}">back to list</a>

    <a href="{{ path('app_accounting_payment_type_edit', {'id': payment_type.id}) }}">edit</a>

    {{ include('accounting/payment_type/_delete_form.html.twig') }}
{% endblock %}
