<form method="post" action="{{ path('app_accounting_payment_type_delete', {'id': payment_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_type.id) }}">
    <button class="btn">Delete</button>
</form>
