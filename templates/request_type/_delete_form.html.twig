<form method="post" action="{{ path('app_request_type_delete', {'id': request_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ request_type.id) }}">
    <button class="btn">Delete</button>
</form>
