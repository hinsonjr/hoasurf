<form method="post" action="{{ path('app_unit_owners_delete', {'id': unit_owner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unit_owner.id) }}">
    <button class="btn">Delete</button>
</form>
