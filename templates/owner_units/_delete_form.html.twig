<form method="post" action="{{ path('app_owner_units_delete', {'id': owner_unit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ owner_unit.id) }}">
    <button class="btn">Delete</button>
</form>
