{% extends "admin-base.html.twig' %}

{% block title %}Owner Units{% endblock %}

{% block content%}
    {{  parent() }}
    <h1>Owner Units</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit_owner.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ unit_owner.startDate ? unit_owner.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ unit_owner.endDate ? unit_owner.endDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>OwnPercent</th>
                <td>{{ unit_owner.ownPercent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_unit_owners_index') }}">back to list</a>

    <a href="{{ path('app_unit_owners_edit', {'id': unit_owner.id}) }}">edit</a>

    {{ include('unit_owners/_delete_form.html.twig') }}
{% endblock %}
