{% extends 'admin-base.html.twig' %}

{% block title %}Maintenance Object{% endblock %}

{% block content %}
    {{  parent() }}
    <h1>Maintenance Object</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maintenance_object.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ maintenance_object.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ maintenance_object.description }}</td>
            </tr>
            <tr>
                <th>Period</th>
                <td>{{ maintenance_object.period }}</td>
            </tr>
            <tr>
                <th>PeriodAmount</th>
                <td>{{ maintenance_object.periodAmount }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ maintenance_object.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('maintenance_object_index') }}">back to list</a>

    <a href="{{ path('maintenance_object_edit', {'id': maintenance_object.id}) }}">edit</a>

    {{ include('maintenance_object/_delete_form.html.twig') }}
{% endblock %}
