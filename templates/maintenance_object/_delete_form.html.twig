<form method="post" action="{{ path('maintenance_object_delete', {'id': maintenance_object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance_object.id) }}">
    <button class="btn">Delete</button>
</form>
