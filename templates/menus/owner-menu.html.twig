<div class="banner">
    <div class="banner-container">
        <div class="container-body">
            <nav id="navbar flex-column" class="navbar navbar-expand-lg navbar-primary-subtle " style="background-color: #e3f2fd;">
                {% block navbar %}
                    <div class="container-fluid">
                        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="navbar-toggler-icon"></span>
                        </button>                
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                            <div class="navbar-nav">
                                {% block navitems %}
                                    <a class="nav-item nav-link" href="{{ path('owner_payments') }}">Payments</a>
                                    <a class="nav-item nav-link" href="{{ path('owner_requests') }}">Requests</a>
                                    <a class="nav-item nav-link" href="{{ path('owner_messages') }}">Messages</a>
                                {% endblock %}
                            </div>

                        </div>
                        <div id="nav-user" class="nav-item dropdown">
                            {%  if app.user %}
                                <a role="button" class="nav-link btn btn-primary dropdown-toggle" href="#" role="button" id="navbarDropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="true">
                                    User {{ app.user.email }} #{{ app.user.id }}
                                </a>
                                <div class="navDropdown dropdown-menu dropdown-menu-right justify-content-right" aria-labelledby="navbarDropdownMenuLink">
                                    <a class="dropdown-item" >{{ app.user.email }}</a>
                                    <a class="dropdown-item" >{{ app.user.roles[0] }}</a>
                                    <a class="dropdown-item " href="/logout">Logout</a>
                                </div>
                            {%  elseif app.request.get('_route') == "login" %}
                                <div class="dropdown-item nav-link"><a href="{{  path('app_register') }}">Register</a></div>
                            {%  else %}
                                <div class="dropdown-item nav-link"><a href="{{  path('login') }}">Login</a></div>
                            {% endif %}
                        </div>
                    </div>
                {% endblock %}
            </nav>
        </div>
    </div>
</div>
