{% extends 'admin-base.html.twig' %}

{% block title %}Request Status{% endblock %}

{% block content%}
    {{  parent() }}
    <h1>Request Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ request_status.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ request_status.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_request_status_index') }}">back to list</a>

    <a href="{{ path('app_request_status_edit', {'id': request_status.id}) }}">edit</a>

    {{ include('request_status/_delete_form.html.twig') }}
{% endblock %}
