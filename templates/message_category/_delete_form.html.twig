<form method="post" action="{{ path('app_message_category_delete', {'id': message_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_category.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
