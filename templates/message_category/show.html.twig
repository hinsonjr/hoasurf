{% extends 'admin-base.html.twig' %}

{% block title %}Message Category{% endblock %}

{% block content %}
    {{  parent() }}
    <h1>Message Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message_category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ message_category.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_message_category_index') }}">back to list</a>

    <a href="{{ path('app_message_category_edit', {'id': message_category.id}) }}">edit</a>

    {{ include('message_category/_delete_form.html.twig') }}
{% endblock %}
