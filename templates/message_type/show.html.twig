{% extends 'admin-base.html.twig' %}

{% block title %}MessageType{% endblock %}

{% block body %}
    {{ parent() }}
    <h1>MessageType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ message_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_message_type_index') }}">back to list</a>

    <a href="{{ path('app_message_type_edit', {'id': message_type.id}) }}">edit</a>

    {{ include('message_type/_delete_form.html.twig') }}
{% endblock %}
