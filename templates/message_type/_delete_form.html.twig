<form method="post" action="{{ path('app_message_type_delete', {'id': message_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_type.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
