<form method="post" action="{{ path('hoa_delete', {'id': hoa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hoa.id) }}">
    <button class="btn">Delete</button>
</form>
