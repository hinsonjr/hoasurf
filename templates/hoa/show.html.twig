{% extends 'admin-base.html.twig' %}

{% block title %}HOA{% endblock %}

{% block content %}
    {{  parent() }}
    <h1>HOA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hoa.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hoa.name }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ hoa.shortName }}</td>
            </tr>
            <tr>
                <th>AdminName</th>
                <td>{{ hoa.adminName }}</td>
            </tr>
            <tr>
                <th>AdminPhone</th>
                <td>{{ hoa.adminPhone }}</td>
            </tr>
            <tr>
                <th>AdminEmail</th>
                <td>{{ hoa.adminEmail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hoa_index') }}">back to list</a>

    <a href="{{ path('hoa_edit', {'id': hoa.id}) }}">edit</a>

    {{ include('hoa/_delete_form.html.twig') }}
{% endblock %}
